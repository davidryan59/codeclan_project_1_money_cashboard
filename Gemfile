# Gemfile

source 'https://rubygems.org'
gem 'sinatra'
gem 'pg'
gem 'date'
ruby '2.4.1'     # Should this be 2.4.1 ?
# ruby '2.4.1'   # This didn't work with bundle install...

# After this done, type 'bundle install' to install into Gemfile.lock
